"use client";
import React from "react";

const Button = () => {
	return (
		<div>
			<button className="btn btn-primary" onClick={() => console.log("asdas")}>asdd</button>
		</div>
	);
};

export default Button;
